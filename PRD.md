Add a scroll speed control slider that updates scroll speed in real time

Add a play/pause button that starts and stops scrolling

Add font size control slider to dynamically adjust text size

Add dark and light mode toggle and persist preference in localStorage